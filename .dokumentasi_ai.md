# Dokumentasi AI - Konteks Estafet Pekerjaan
> File ini dibuat/diupdate otomatis oleh AI Agent untuk memudahkan handoff antar sesi/akun.
> Format: LOG KRONOLOGIS dari yang terbaru ke terlama.

---

## SESSION: 2026-04-06 — SELESAI ✅ (PCareCekKartu.java Refactoring)

### Status: IMPLEMENTASI SELESAI
File yang diubah: `src/bridging/PCareCekKartu.java`

### Ringkasan Perubahan yang Dilakukan

#### 1. Cache Instance Variables (baris ~116)
Ditambah 5 variabel baru untuk pass data dari Task 0 ke Task 1:
```java
private String kodePoliBPJSCache = "", tglRegistrasiCache = "",
               noKartuKirimCache = "", noRMCache = "", namaPasienCache = "";
```

#### 2. Alur `SimpanPendaftaranPCare()` Dirombak (baris ~5870)
**Sebelum**: Add Antrean (Task 0) → langsung Add PCare
**Sesudah**: Task 0 → Task 1 (Update Hadir) → Add PCare
- Jika Task 0 gagal: dialog konfirmasi ke user
- Jika user paksa lanjut: log `!!! PELANGGARAN ALUR BPJS !!!` ke trackersql

#### 3. `SimpanAntrianOnSite()` Direfactor (baris ~6130–6290)
- Semua `catatTrackerGagal()` diganti dengan `catatTrackerBPJS()`
- Response 200: set 5 cache variables + audit trail + simpanTaskId("0")
- Response 201 "sudah terdaftar": bypass + set cache + audit trail + simpanTaskId("0")
- Response 201 lain / error: audit trail saja (statusantrean=false)
- INSERT IGNORE dipakai agar aman dari duplicate key pada kasus 201-bypass

#### 4. Method Baru: `UpdateHadirOnSite()` (baris ~6331)
Task 1 - Update Hadir. Dipanggil HANYA setelah Task 0 200 OK.
Endpoint: `POST {URLMOBILEJKNFKTP}/antrean/panggil`
Payload: `{tanggalperiksa, kodepoli, nomorkartu, status: 1, waktu: currentTimeMillis}`
- Sukses (200): audit trail + simpanTaskId("1")
- Gagal: audit trail saja, TIDAK blocking (tetap lanjut Add PCare)

#### 5. Method Baru: `simpanTaskId(noRawat, taskId)` (baris ~6310)
Menggantikan `simpanTask0()`. Generic, pakai INSERT IGNORE.
Menyimpan ke: `referensi_mobilejkn_bpjs_taskid` (no_rawat, taskid, waktu saja — NO ALTER TABLE)

#### 6. Method Baru: `catatTrackerBPJS(...)` (baris ~6407)
Menggantikan `catatTrackerGagal()`. Terpusat, format standar.
Pakai koneksi independen (`koneksiDB.condb()`) agar tidak collision dengan ResultSet aktif.
Format log di `trackersql.sqle`:
```
[LOG BRIDGING BPJS] No.Rawat: ... | No.RM: ... | Pasien: ... | Task: ... | PAYLOAD: ... | RESPONSE BPJS: ...
```

### Method yang Dihapus
- `simpanTask0(String noRawat)` → diganti `simpanTaskId(String noRawat, String taskId)`
- `catatTrackerGagal(String noRawat, String namaPasien, String pesanError)` → diganti `catatTrackerBPJS(...)`

### Alur Final (Wajib, Syarat Mutlak)
```
Klik Simpan (BtnSimpan) → insertPasien() → SimpanPendaftaranPCare()
  ├─ [TASK 0] POST /antrean/add → response 200/201-bypass
  │     └─ Set cache → simpanTaskId("0") → catatTrackerBPJS(Task 0 Sukses)
  ├─ [TASK 1] POST /antrean/panggil → response 200
  │     └─ simpanTaskId("1") → catatTrackerBPJS(Task 1 Sukses)
  └─ [ADD PCARE] POST /pendaftaran (via ApiPcare)
```

### Format Log yang Dihasilkan di trackersql

**Task 0 Sukses**:
```
[LOG BRIDGING BPJS] No.Rawat: 2026/04/06/000012 | No.RM: 012905 | Pasien: BUDI SANTOSO | Task: Task 0 - Add Antrean Sukses (200) | PAYLOAD: {...} | RESPONSE BPJS: {"metadata":{"code":200,"message":"Ok"}}
```

**Task 1 Sukses**:
```
[LOG BRIDGING BPJS] No.Rawat: 2026/04/06/000012 | No.RM: 012905 | Pasien: BUDI SANTOSO | Task: Task 1 - Update Hadir Sukses (200) [SOAP] | PAYLOAD: {"tanggalperiksa":"2026-04-06","kodepoli":"002","nomorkartu":"...","status":1,"waktu":...} | RESPONSE BPJS: {"metadata":{"code":200,"message":"Ok"}}
```

**User Paksa Lanjut (Antrean Gagal)**:
```
!!! PELANGGARAN ALUR BPJS !!! USER admisi01 BERSIKERAS MELANJUTKAN SIMPAN REGISTRASI PCARE MESKIPUN ADD ANTREAN (TASK 0) GAGAL!!! No.Rawat: ... | No.RM: ... | Pasien: ...
```

---

## Hal yang Perlu Dilakukan User (Tindak Lanjut)

1. **Matikan background service PHP untuk Task 1** — Task 1 sekarang dilakukan langsung saat admisi.
   Background service yang masih mengirim Task 1 akan menyebabkan double-send.

2. **Build & Test di NetBeans** — Kompilasi ulang dan test alur registrasi pasien BPJS.

3. **Verifikasi di DB production** setelah 1 pasien BPJS didaftarkan:
   ```sql
   -- Cek audit trail
   SELECT * FROM trackersql WHERE sqle LIKE '%LOG BRIDGING BPJS%' ORDER BY tanggal DESC LIMIT 10;
   -- Cek task ID
   SELECT * FROM referensi_mobilejkn_bpjs_taskid ORDER BY waktu DESC LIMIT 10;
   ```

---

## Konteks Sistem (Referensi)

### Database Production
- IP: `192.168.196.103` | User: `client` | Pass: `epotoransu` | DB: `sik_master`

### File Utama yang Relevan
| File | Deskripsi |
|------|-----------|
| `src/bridging/PCareCekKartu.java` | Form Pendaftaran PCare (admisi) — **SUDAH DIUPDATE** |
| `src/bridging/ApiMobileJKNFKTP.java` | Helper API Mobile JKN (getHmac, getRest, GetUTCdatetimeAsString) |
| `src/bridging/ApiPcare.java` | Helper API PCare |

### Tabel DB yang Digunakan
| Tabel | Fungsi |
|-------|--------|
| `trackersql` | Audit trail — semua payload+response BPJS (mediumtext, utf8mb4) |
| `referensi_mobilejkn_bpjs_taskid` | Dokumentasi task ID (no_rawat, taskid, waktu — NO ALTER TABLE) |

### Endpoint API MobileJKN FKTP
- **Add Antrean (Task 0)**: `POST {URLMOBILEJKNFKTP}/antrean/add`
- **Update Hadir (Task 1)**: `POST {URLMOBILEJKNFKTP}/antrean/panggil`
- **Batal Antrean (Task 2)**: `POST {URLMOBILEJKNFKTP}/antrean/batal` (tidak dihandle di sini)

### Conversation ID sesi ini
`b78656b8-c958-409c-aad5-03e88e0c5015`
